<div id="<%= dom_id post %>">

  <p>
    <strong>Title:</strong>
    <%= post.title %>
  </p>

  <p>
    <strong>Body:</strong>
    <%= post.body %>
  </p>

  <p>
  <% if user_signed_in?%>
    <i class="font-weight-light"> Authored by: <%= post.user.name %>  </i>
    <% end %>
  </p>

</div>
